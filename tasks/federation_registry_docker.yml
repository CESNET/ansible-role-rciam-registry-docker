---
- name: Create directories
  ansible.builtin.file:
    path: "{{ item.path }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0755') }}"
    state: directory
  loop: "{{ federation_registry_dirs }}"

- name: Copy files
  block:
    # Copying files in individual tasks to run only the required handlers
    - name: Copy compose.yaml
      ansible.builtin.template:
        src: "templates/compose.yaml.j2"
        dest: "{{ federation_registry_config_path }}/compose.yaml"
        mode: "0640"

    - name: Copy files for frontend
      ansible.builtin.copy:
        src: "files/{{ deployment_id }}/frontend-config"
        dest: "{{ federation_registry_config_path }}"
        owner: root
        group: 101
        mode: '0640'
      notify: Reload NGINX in frontend

    - name: Copy files for backend-api
      ansible.builtin.copy:
        src: "files/{{ deployment_id }}/backend-config"
        dest: "{{ federation_registry_config_path }}"
        owner: "root"
        group: 1000
        mode: "0640"
      notify: Restart backend-api

    - name: Copy templates for backend-api
      ansible.builtin.template:
        src: "{{ item.src }}.j2"
        dest: "{{ item.dest | default(item.src) }}"
        owner: "{{ item.owner | default('root') }}"
        group: "{{ item.group | default(1000) }}"
        mode: "{{ item.mode | default('0640') }}"
      notify: Restart backend-api
      with_list:
        - src: "templates/backend-config/db-config.json"
          dest: "{{ federation_registry_config_path }}/backend-config/db-config.json"
        - src: "templates/backend-config/.env"
          dest: "{{ federation_registry_config_path }}/backend-config/.env"

    - name: Copy files for deployer
      ansible.builtin.copy:
        src: "files/{{ deployment_id }}/deployer"
        dest: "{{ federation_registry_config_path }}"
        owner: "root"
        group: 1000
        mode: "0640"
      notify: Restart deployers

    - name: Copy templates for RabbitMQ
      ansible.builtin.template:
        src: "{{ item.src }}.j2"
        dest: "{{ item.dest | default(item.src) }}"
        owner: "{{ item.owner | default('root') }}"
        group: "{{ item.group | default(415) }}"
        mode: "{{ item.mode | default('0640') }}"
      notify: Restart rabbitmq
      with_list:
        - src: "templates/rabbitmq/rabbitmq.conf"
          dest: "{{ federation_registry_config_path }}/rabbitmq/rabbitmq.conf"
        - src: "templates/rabbitmq/initial-state-definitions.json"
          dest: "{{ federation_registry_config_path }}/rabbitmq/initial-state-definitions.json"

    - name: Copy files for RabbitMQ agents
      ansible.builtin.copy:
        src: "{{ item.config }}"
        dest: "{{ item.config_dest | default([federation_registry_config_path, 'rabbitmq-agent'] | join('/')) }}"
        owner: "{{ item.owner | default('root') }}"
        group: "{{ item.group | default('root') }}"
        mode: "{{ item.mode | default('0644') }}"
      notify: Restart rabbitmq-agents
      tags:
        - configuration
      with_list: "{{ federation_registry_rabbitmq_agents }}"

- name: Ensure log file exists for the backend-api
  ansible.builtin.file:
    access_time: preserve
    modification_time: preserve
    path: "{{ federation_registry_log_path }}/backend-api-errors.log"
    state: touch
    owner: 1000
    mode: 0750

- name: Ensure federation_registry docker-compose has started
  community.docker.docker_compose_v2:
    project_src: "{{ federation_registry_config_path }}"
    state: present
    remove_orphans: true
    pull: always
    wait: true
  register: docker_compose_status

- name: Show restarted containers
  ansible.builtin.debug:
    msg: "{{ docker_compose_status | community.general.json_query('actions[?status==`Recreate`].id') }}"
  ignore_errors: "{{ ansible_check_mode }}"
  when: docker_compose_status | community.general.json_query('actions[?status==`Recreate`].id') != []
  changed_when: docker_compose_status | community.general.json_query('actions[?status==`Recreate`].id') != []

- name: flush handlers
  meta: flush_handlers